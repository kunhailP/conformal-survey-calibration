[project]
name = "dac"
version = "0.1.0"
description = "Targets and assumptions for conformal prediction on survey-estimated curves"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "scipy>=1.10", "pandas>=2.0", "matplotlib>=3.7"]

[tool.setuptools.packages.find]
where = ["src"]
